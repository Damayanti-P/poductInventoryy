export interface User {
    id?: number;  // Optional for new users
    email: string;
    password: string;
    firstName: string;
    lastName: string;
    location: string;
    mobileNumber: string;
  }
  